services:
  - type: web
    name: NexusBot
    runtime: node
    buildCommand: npm install && pip3 install ddgs
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
